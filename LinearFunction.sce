clear
clc()
disp("Linear function a*x+b=0")
a=input("Enter a: ");
b=input("Enter b: ");
x=input("Enter x: ");
if (a*x+b)==0 then
    disp("X is the zero site.")
else
    disp("X is not the zero site.")    
end
